const {
  regularProviderRegistration,
} = require("./regularProviderRegistration");
const {
  InvitedProviderRegistration,
} = require("./InvitedProviderRegistration");

module.exports = { regularProviderRegistration, InvitedProviderRegistration };
